APENAS NO SCANNER

flex scanner.l
gcc lex.yy.c -o scanner
./scanner [sort.txt] // opcional passar o arquivo

NO PARSER (no MSYS MINGW)


Se possui Makefile e estiver atualizado, rode:

make run

Senão:

bison -d parser.y
flex scanner.l
gcc -o parser parser.tab.c lex.yy.c arvore.c
./parser.exe sort.txt
